[tool.ruff]
line-length = 100
target-version = "py311"
exclude = ["quotes/migrations"]

[tool.ruff.lint]
select = ["E", "F", "I", "W"]

[tool.ruff.lint.per-file-ignores]
# Citations sources longues et incompressibles (données, pas du code).
"quotes/management/commands/seed_quotes.py" = ["E501"]
